html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/styles/styles.css")
        title Guess Mine
    body
        .loginBox
            form#jsLogin
                input(placeholder="Whats your nickname" type="text")
    
        .gameContainer#jsGame
            .canvas
                canvas#jsCanvas
                .controls
                    .controls__range
                        input#jsRange(type="range", min="0.1",  max="5",  step="0.1", value="2.5")
                    .controls__btns
                        button#jsMode STROKE
                        button#jsSave SAVE
                    .colorBox#jsColorBox
                        .color(style="background-color: #2c2c2c;") 
                        .color(style="background-color: white;") 
                        .color(style="background-color: #ff3b30;") 
                        .color(style="background-color: #ff9500;") 
                        .color(style="background-color: #ffcc00;") 
                        .color(style="background-color: #4cd963;") 
                        .color(style="background-color: #5ac8fa;") 
                        .color(style="background-color: #0579ff;") 
                        .color(style="background-color: #5856d6;") 
            .chat
                .player__board#jsPBoard
                ul.chat__messages#jsMessages
                form.chat__form#jsSendMsg
                    input(placeholder="sendMessage" type="text")

        //- 스크립트. 은 바로 스크립트를 작성할 수 있다.
        //- window.global 변수에 전역변수를 선언
        //- !{event}와 #{event} 차이는 뭐냐?  파싱을 다르게 해주는듯.
        script(src="/socket.io/socket.io.js")
        script.
            window.global = !{event}
            window.socket = io("/")
        script(src="/js/main.js")

        